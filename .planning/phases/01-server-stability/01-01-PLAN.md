# Plan: 01-01 - Fix Logging and Audit

## Goal
Fix the `Context.info` positional argument bug and audit all logging calls in `server.py` to ensure compatibility with the `FastMCP` framework.

## Proposed Changes

### `src/synapse_mcp/server.py`
- Convert all `ctx.info()` calls that use multiple arguments into single-string f-strings.
- Audit for any other `ctx.error` or `ctx.warn` calls (though none were found in initial grep, I'll do a final check).

## Verification Plan

### Automated Tests
- Run `tests/debug_test.py` to ensure the server still starts and tools don't crash.
- Run `tests/test_basic.py`.

### Manual Verification
- I'll simulate a call to `generate_insights` if possible, or at least verify the code change fixes the reported error (positional arguments).

## Must-Haves
- [ ] All `ctx.info` calls in `server.py` use only one positional argument.
- [ ] No regressions in tool functionality.
