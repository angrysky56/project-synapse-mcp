{
  "_comment": "All configuration (Neo4j credentials, LLM/embedding providers, wiki paths) lives in the project's .env file — no env block needed here. The server loads <project-root>/.env on import. Any env vars set here would OVERRIDE the .env, so only add them for deliberate per-launch overrides.",
  "mcpServers": {
    "project-synapse": {
      "command": "uv",
      "args": [
        "--directory",
        "/absolute/path/to/your/project-synapse-mcp",
        "run",
        "python",
        "-m",
        "synapse_mcp.server"
      ]
    }
  }
}
